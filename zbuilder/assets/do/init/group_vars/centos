ZBUILDER_PROVIDER:
  DNS: "{{ GLOBAL_PROVIDER }}"
  CLOUD: "{{ GLOBAL_PROVIDER }}"
  VM_OPTIONS:
    size_slug: "{{ GLOBAL_SIZE }}"
    region: "{{ GLOBAL_REGION }}"
    sshkey: "{{ GLOBAL_SSHKEY }}"
    image: centos-7-x64
