ZBUILDER_PROVIDER:
  DNS: "{{ GLOBAL_PROVIDER }}"
  CLOUD: "{{ GLOBAL_PROVIDER }}"
  VM_OPTIONS:
    sysuser: "{{ GLOBAL_SYSUSER }}"
    memory: "{{ GLOBAL_MEMORY }}"
    vcpus: "{{ GLOBAL_VCPUS }}"
    disks: "{{ GLOBAL_DISKS }}"
    box: bento/centos-6.9
