ZBUILDER_PROVIDER:
  DNS: vagrant
  CLOUD: vagrant
  VM_OPTIONS:
    sysuser: "{{ GLOBAL_SYSUSER }}"
    box: bento/centos-6.9
    memory: "{{ GLOBAL_MEMORY }}"
    vcpus: "{{ GLOBAL_VCPUS }}"
    disks: "{{ GLOBAL_DISKS }}"
