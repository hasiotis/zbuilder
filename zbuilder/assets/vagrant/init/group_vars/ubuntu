ZBUILDER_PROVIDER:
  DNS: vagrant
  CLOUD: vagrant
  VM_OPTIONS:
    sysuser: "{{ GLOBAL_SYSUSER }}"
    box: bento/ubuntu-16.04
    memory: "{{ GLOBAL_MEMORY }}"
    vcpus: "{{ GLOBAL_VCPUS }}"
    disks: "{{ GLOBAL_DISKS }}"
